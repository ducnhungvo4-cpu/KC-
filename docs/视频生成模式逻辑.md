# 视频生成模式逻辑

## 模式

首尾帧

图片 × 2 → 首帧、尾帧 → 视频

全能参考

图片 / 视频 / 音频 → `@` 插入提示词 → 视频

## 模型

Seedance 2.0 → 首尾帧、全能参考

Seedance 2.0 Fast → 首尾帧、全能参考

## 引用

连接素材 → 输入 `@` → 按类型选择素材 → 缩略图插入文字间隙

收起提示词框 → 支持 `@` 引用

展开提示词框 → 使用相同的 `@` 引用逻辑

断开素材连接 → 对应引用失效 → 禁止生成

## 画布文件

右上角文件夹 → 导入画布 / 导出画布
